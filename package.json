{
  "name": "mcp-skills-enrichedlab",
  "version": "0.0.0",
  "private": true,
  "description": "MCP servers and Claude skills for enhanced development workflows",
  "author": "Richard Stoker",
  "license": "MIT",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "clean": "npm run clean --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "engines": {
    "node": ">=24.0.0"
  }
}
